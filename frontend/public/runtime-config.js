window.__BOAI_CONFIG__ = {
  API_BASE_URL: '',
  // 你的 update.json 地址，建议放你自己的服务器，优先级最高。
  // 示例: ['https://your-domain.com/boai/update.json']
  UPDATE_MANIFEST_URLS: [],
  // 备用的“最新版本接口”地址，只有在 update.json 拿不到时才会尝试。
  // 示例: ['https://your-domain.com/api/boai/releases/latest']
  UPDATE_RELEASE_API_URLS: [],
  // APK 下载代理前缀，适合 GitHub 在国内访问不稳定时做兜底。
  // 示例: ['https://ghproxy.net/', 'https://mirror.ghproxy.com/']
  UPDATE_DOWNLOAD_PROXY_PREFIXES: [],
  // 直接可下载的 APK 地址，建议放你自己服务器的直链。
  // 示例: ['https://your-domain.com/boai/boai-music-latest.apk']
  UPDATE_DOWNLOAD_URLS: [],
}
